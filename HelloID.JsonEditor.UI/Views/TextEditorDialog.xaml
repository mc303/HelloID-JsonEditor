<Window x:Class="HelloID.JsonEditor.UI.Views.TextEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Editor"
        Height="900" Width="700"
        MinHeight="900" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False">

    <DockPanel Margin="8">
        <!-- Bottom Controls -->
        <Grid DockPanel.Dock="Bottom" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left side: Variable selector and Insert button -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Variables:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="VariableComboBox"
                          Width="300"
                          Height="30"
                          IsEditable="True"
                          IsTextSearchEnabled="False"
                          SelectionChanged="VariableComboBox_SelectionChanged"
                          VerticalAlignment="Center"
                          Margin="0,0,8,0"
                          GotFocus="VariableComboBox_GotFocus"/>
                <Button Content="Insert" Width="80" Height="30"
                        Click="Insert_Click"
                        VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Right side: OK and Cancel buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Width="80" Height="30" Margin="0,0,8,0"
                        Click="OK_Click" IsDefault="True"/>
                <Button Content="Cancel" Width="80" Height="30"
                        Click="Cancel_Click" IsCancel="True"/>
            </StackPanel>
        </Grid>

        <!-- Text Editor -->
        <TextBox x:Name="EditorTextBox"
                 TextWrapping="NoWrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"/>
    </DockPanel>
</Window>
